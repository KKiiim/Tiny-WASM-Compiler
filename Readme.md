## Set Env

```sh
sudo apt-get update &&
sudo apt-get install qemu-user gcc-aarch64-linux-gnu g++-aarch64-linux-gnu cmake make clang-tidy clang-format cspell@latest &&
npm ci && npm run build:demo
```
